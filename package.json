{
  "name": "jjs-academic-homepage",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync:cv": "node scripts/sync-cv.mjs",
    "dev": "npm run sync:cv && astro dev",
    "build": "npm run sync:cv && astro build",
    "preview": "astro preview",
    "check": "astro check"
  },
  "dependencies": {
    "astro": "^5.3.0"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
